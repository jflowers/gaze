## Why

The "multiple target functions detected" warning provides no guidance on how to resolve it. Users see the warning but don't know why setup helpers are detected as targets or how to restructure tests. The README now has a "How Target Inference Works" section (PR #57) but the warning doesn't reference it.

## What Changes

Update the warning message to include a documentation reference.

## Constitution Alignment

All PASS. Minimal change improving Actionable Output (Principle III).
